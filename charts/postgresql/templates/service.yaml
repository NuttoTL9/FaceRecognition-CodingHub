apiVersion: v1
kind: Service
metadata:
  name: postgresql
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.postgresql.port }}
      targetPort: {{ .Values.postgresql.port }}
      protocol: TCP
      name: postgres
  selector:
    app: postgresql
